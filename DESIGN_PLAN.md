# План доработок дизайна HH TextGen Chrome Extension

## Статус: выполнено (задачи 1–6), задача 7 — требует дизайн-решения

---

## 1. SVG-иконки вместо Unicode

**Что:** Заменить все Unicode-символы (⚙, 🗑, →, ⌘, ✓, ⚡, 📝) на inline SVG из набора Lucide Icons (16×16 / 20×20).

**Где:**
- `popup.html`: header buttons (settings, clear), send button, welcome icon, ad empty state icon
- `popup.js`: copy button text (`⌘ Копировать` → SVG clipboard, `✓ Скопировано` → SVG check)

**Почему:** Unicode-эмодзи рендерятся по-разному на macOS/Windows/Linux. SVG — pixel-perfect, одинаковые везде, масштабируются без потерь.

**Объём:** ~20 inline SVG, только popup.html + popup.js. Без внешних файлов (всё inline).

---

## 2. Цветовая идентификация площадок

**Что:** Каждая площадка получает свой accent-цвет: VK `#0077FF`, Яндекс `#FC3F1D`, Telegram `#26A5E4`.

**Где:**
- `popup.html` (CSS): новые переменные `--vk`, `--yandex`, `--tg` + стили `.cb-pill[data-platform]` для checked-state в цвете площадки
- `popup.html` (HTML): добавить `data-platform="vk|yandex|tg"` на каждый `.cb-pill`
- `popup.js`: при рендере `.ad-card` — подкрашивать `.ad-card-platform` цветом площадки, тонкий `border-left: 3px solid` на карточке

**CSS-переменные:**
```css
--vk: #0077FF;
--vk-dim: rgba(0, 119, 255, 0.12);
--yandex: #FC3F1D;
--yandex-dim: rgba(252, 63, 29, 0.12);
--tg: #26A5E4;
--tg-dim: rgba(38, 165, 228, 0.12);
```

**Объём:** CSS-переменные + data-атрибуты в HTML + маппинг в JS (~30 строк).

---

## 3. Character count как progress bar

**Что:** Под каждым текстовым полем в карточке результата — тонкая полоска (height: 3px), заполненная пропорционально использованию лимита.

**Визуал:**
- 0–90% лимита: `var(--green)` (зелёный)
- 90–100%: `var(--yellow)` (жёлтый)
- >100%: `var(--accent)` (красный)

**Где:**
- `popup.html` (CSS): `.ad-char-bar` — контейнер + `.ad-char-bar-fill` — заполнение
- `popup.js`: `renderField()` — генерировать HTML progress bar, вычислять width %

**Сохранить** текстовый счётчик `42 / 81` рядом — bar дополняет, не заменяет.

**Объём:** ~15 строк CSS + ~10 строк JS в `renderField()`.

---

## 4. prefers-reduced-motion

**Что:** Обёртка `@media (prefers-reduced-motion: reduce)` которая отключает все анимации и transitions.

**Где:** `popup.html` (CSS), в конце стилей.

**Реализация:**
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

**Объём:** 6 строк CSS.

---

## 5. View Transitions для смены режимов

**Что:** Crossfade при переключении Chat ↔ Ad Texts через `document.startViewTransition()`.

**Где:** `popup.js`, в обработчике клика по табу.

**Реализация:**
```js
if (document.startViewTransition) {
    document.startViewTransition(() => { /* переключение display */ });
} else {
    /* fallback: мгновенное переключение */
}
```

**CSS:**
```css
::view-transition-old(root), ::view-transition-new(root) {
  animation-duration: 0.15s;
}
```

**Объём:** ~10 строк JS + 4 строки CSS. Работает в Chrome 111+.

---

## 6. Favicon для вкладки

**Что:** Добавить `<link rel="icon">` с маленькой SVG-иконкой (молния или текстовый курсор).

**Где:** `popup.html` — в `<head>`.

**Реализация:** Inline SVG data URI, без отдельного файла:
```html
<link rel="icon" href="data:image/svg+xml,<svg ...>...</svg>">
```

**Объём:** 1 строка HTML.

---

## 7. Обновление extension icon (icon.png)

**Что:** Перерисовать icon.png (48×48 и 128×128) в стиле UI — тёмный фон, accent-цвет, минималистичный символ.

**Где:** `chrome-extension/icon.png` (или split на `icon-48.png` + `icon-128.png` в manifest.json).

**Объём:** Дизайн-задача. Можно сгенерировать как SVG → экспорт в PNG.

---

## Порядок реализации

| # | Задача | Импакт | Усилие | Статус |
|---|--------|--------|--------|--------|
| 1 | SVG-иконки | высокий | средний | DONE |
| 2 | Цвета площадок | высокий | низкий | DONE |
| 3 | Progress bars | средний | низкий | DONE |
| 4 | reduced-motion | низкий | мин. | DONE |
| 5 | View Transitions | низкий | мин. | DONE |
| 6 | Favicon | мин. | мин. | DONE |
| 7 | Extension icon | средний | средний | TODO — требует дизайн |

Задачи 1–3 независимы, можно делать параллельно.
Задачи 4–6 — по 5 минут каждая, можно в одном коммите.
Задача 7 — отдельно, требует дизайн-решения.

---

## После реализации

- Обновить `local-preview/index.html` (синхронизировать CSS)
- Пересобрать `HH_TextGen_Chrome.zip`
- Скопировать на Desktop
